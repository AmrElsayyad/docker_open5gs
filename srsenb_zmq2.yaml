version: '3'
services:
  srsenb_zmq2:
    image: docker_srslte
    container_name: srsenb2_zmq2
    stdin_open: true
    tty: true
    privileged: true
    volumes:
      - ./srslte2:/mnt/srslte
      - /etc/timezone:/etc/timezone:ro
      - /etc/localtime:/etc/localtime:ro
    env_file:
      - .env
    environment:
      - COMPONENT_NAME=enb_zmq
    expose:
      - "36412/sctp"
      - "2152/udp"
      - "2000/tcp"
      - "2001/tcp"
    networks:
      default:
        ipv4_address: ${SRS_ENB2_IP}
networks:
  default:
    external:
      name: docker_open5gs_default
